#include "sliding.h"
/*  Slide all values of array up
*Note that the matrix is stored in a 1D array in row-major order
*if the value of a cell is -1, it is empty. Else is it not empty
*traverse each col and see if you can move the cells up. Then move on and do the same for the next column
*/
void slide_up(int* my_array, int rows, int cols){


    return;
}
